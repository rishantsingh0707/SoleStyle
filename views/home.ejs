<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Premium Shoes Collection</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<style>
  /* Modern Shoes E-commerce Styling - Fixed Version */

  :root {
    --primary-color: #2c3e50;
    --secondary-color: #e74c3c;
    --accent-color: #f39c12;
    --success-color: #27ae60;
    --background-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --card-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    --card-shadow-hover: 0 20px 40px rgba(0, 0, 0, 0.15);
    --text-primary: #2c3e50;
    --text-secondary: #7f8c8d;
    --border-radius: 16px;
    --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Global Styles */
  body {
    font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    background: var(--background-gradient);
    min-height: 100vh;
    color: var(--text-primary);
    line-height: 1.6;
    margin: 0;
    padding: 0;
  }

  .page-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  /* Header Styling */
  h1 {
    text-align: center;
    font-size: 3rem;
    font-weight: 700;
    color: white;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
    margin: 2rem 0;
    letter-spacing: -0.02em;
  }

  .h2 {
    font-size: 2.5rem;
    font-weight: 600;
    color: white;
    text-align: center;
    margin-bottom: 3rem;
    position: relative;
  }

  .h2::after {
    content: "";
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 4px;
    background: var(--accent-color);
    border-radius: 2px;
  }

  /* Alert Styling */
  .alert {
    border: none;
    border-radius: var(--border-radius);
    padding: 1rem 1.5rem;
    margin-bottom: 2rem;
    font-weight: 500;
    box-shadow: var(--card-shadow);
  }

  .alert-danger {
    background: linear-gradient(135deg, #ff6b6b, #ee5a52);
    color: white;
  }

  .alert-success {
    background: linear-gradient(135deg, #51cf66, #40c057);
    color: white;
  }

  /* Filter Form Styling */
  form {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-radius: var(--border-radius);
    padding: 2rem;
    margin-bottom: 3rem;
    box-shadow: var(--card-shadow);
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
    align-items: end;
  }

  form label {
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 0.5rem;
    display: block;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  form input {
    width: 100%;
    padding: 0.75rem 1rem;
    border: 2px solid #e9ecef;
    border-radius: 12px;
    font-size: 1rem;
    transition: var(--transition);
    background: white;
  }

  form input:focus {
    outline: none;
    border-color: var(--primary-color);
    box-shadow: 0 0 0 3px rgba(44, 62, 80, 0.1);
    transform: translateY(-2px);
  }

  form button {
    background: linear-gradient(135deg, var(--primary-color), #34495e);
    color: white;
    border: none;
    padding: 0.75rem 2rem;
    border-radius: 12px;
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    transition: var(--transition);
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  form button:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 25px rgba(44, 62, 80, 0.3);
  }

  /* Card Styling */
  .custom-card {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border: none;
    border-radius: var(--border-radius);
    overflow: hidden;
    transition: var(--transition);
    box-shadow: var(--card-shadow);
    position: relative;
    height: 100%;
  }

  .custom-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--secondary-color), var(--accent-color));
    transform: scaleX(0);
    transition: var(--transition);
  }

  .custom-card:hover::before {
    transform: scaleX(1);
  }

  .custom-card:hover {
    transform: translateY(-10px);
    box-shadow: var(--card-shadow-hover);
  }

  .card-link {
    text-decoration: none;
    color: inherit;
    display: block;
  }

  .card-body {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .card-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 1rem;
    line-height: 1.3;
  }

  .card-img-top {
    width: 100%;
    height: 200px;
    object-fit: cover;
    transition: var(--transition);
    border-radius: 12px;
    margin-bottom: 1rem;
  }

  .card-img-top:hover {
    transform: scale(1.05);
  }

  .no-image {
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    font-weight: 600;
    font-size: 1.1rem;
    height: 200px;
    border-radius: 12px;
  }

  .card-subtitle {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--secondary-color);
    margin-top: auto;
  }

  /* Button Styling */
  .card-buttons {
    padding: 1rem 1.5rem;
    display: flex;
    gap: 0.75rem;
    border-top: 1px solid rgba(0, 0, 0, 0.1);
    background: rgba(0, 0, 0, 0.02);
  }

  .btn {
    padding: 0.5rem 1.25rem;
    border-radius: 8px;
    font-weight: 600;
    font-size: 0.9rem;
    border: none;
    cursor: pointer;
    transition: var(--transition);
    text-transform: uppercase;
    letter-spacing: 0.3px;
    flex: 1;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--primary-color), #34495e);
    color: white;
  }

  .btn-primary:hover {
    color: #000000;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(44, 62, 80, 0.3);
  }

  .btn-danger {
    background: linear-gradient(135deg, var(--secondary-color), #c0392b);
    color: rgb(0, 0, 0);
  }

  .btn-danger:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(231, 76, 60, 0.3);
  }

  /* No Items Message */
  .no-items {
    background: rgba(255, 255, 255, 0.9);
    padding: 3rem;
    border-radius: var(--border-radius);
    text-align: center;
    font-size: 1.2rem;
    font-weight: 500;
    color: var(--text-secondary);
    box-shadow: var(--card-shadow);
    backdrop-filter: blur(10px);
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .page-container {
      padding: 0 15px;
    }

    h1 {
      font-size: 2rem;
    }

    .h2 {
      font-size: 1.8rem;
    }

    form {
      grid-template-columns: 1fr;
      padding: 1.5rem;
    }

    .card-buttons {
      flex-direction: column;
    }
  }

  @media (max-width: 480px) {
    h1 {
      font-size: 1.5rem;
    }

    .card-body {
      padding: 1rem;
    }

    .card-buttons {
      padding: 1rem;
    }
  }

  /* Animations */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .custom-card {
    animation: fadeInUp 0.6s ease forwards;
  }

  /* Focus Styles for Accessibility */
  button:focus,
  input:focus,
  a:focus {
    outline: 2px solid var(--accent-color);
    outline-offset: 2px;
  }
</style>

<body>
  <%- include("navbar") %>
    <div class="mt-4"></div>

    <h1>Premium Shoes Collection</h1>
    <div class="page-container mt-4">
      <% if (error && error.length> 0) { %>
        <div class="alert alert-danger">
          <%= error[0] %>
        </div>
        <% } %>

          <% if (success && success.length> 0) { %>
            <div class="alert alert-success">
              <%= success[0] %>
            </div>
            <% } %>

              <h2 class="mb-4 text-center h2">Discover Amazing Footwear</h2>

              <!-- Enhanced Filter Form -->
              <form method="GET" action="/">
                <div>
                  <label>Category:</label>
                  <input type="text" name="category" placeholder="e.g. running shoes, sneakers"
                    value="<%= category || '' %>">
                </div>
                <div>
                  <label>Min Price:</label>
                  <input type="number" name="minPrice" placeholder="₹0" value="<%= minPrice || '' %>">
                </div>
                <div>
                  <label>Max Price:</label>
                  <input type="number" name="maxPrice" placeholder="₹10000" value="<%= maxPrice || '' %>">
                </div>
                <div>
                  <button type="submit">Find Shoes</button>
                </div>
              </form>

              <div class="row row-cols-1 row-cols-md-3 g-4">
                <% if (items && items.length> 0) { %>
                  <% items.forEach(item=> { %>
                    <div class="col">
                      <!-- Fixed card structure and link placement -->
                      <div class="card h-100 custom-card">
                        <a href="/items/view/<%= item._id %>" class="card-link">
                          <div class="card-body">
                            <h5 class="card-title">
                              <%= item.name %>
                            </h5>
                            <% if (item.imageUrl && item.imageUrl.length> 0) { %>
                              <% item.imageUrl.forEach(img=> { %>
                                <img src="<%= img %>" class="card-img-top mb-2" alt="<%= item.name %>">
                                <% }) %>
                                  <% } else { %>
                                    <div class="card-img-top d-flex align-items-center justify-content-center no-image">
                                      👟 No Image Available
                                    </div>
                                    <% } %>
                                      <h6 class="card-subtitle">₹<%= item.price %>
                                      </h6>
                          </div>
                        </a>

                        <% if (user && item.createdBy && item.createdBy._id && item.createdBy._id.toString()===(user._id
                          ? user._id.toString() : user.id)) { %>
                          <div class="card-buttons">
                            <form action="/items/edit/<%= item._id %>" style="display:inline;">
                              <button type="submit" class="btn btn-primary">Edit</button>
                            </form>
                            <form action="/items/<%= item._id %>?_method=DELETE" method="POST" style="display:inline;">
                              <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                          </div>
                          <% } %>
                      </div>
                    </div>
                    <% }) %>
                      <% } else { %>
                        <div class="col-12">
                          <p class="text-center no-items">
                            No shoes available right now 👟<br>
                            <small>Check back soon for amazing deals!</small>
                          </p>
                        </div>
                        <% } %>
              </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>